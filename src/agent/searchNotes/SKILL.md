# 搜索与读取

## 适用

- 用户明确要求查找当前笔记本中的内容。
- 目标页面不明确，需要先定位。
- 回答依赖某篇页面的完整正文。

## 执行

- 搜索范围仅限当前绑定笔记本。
- 当前页面已明确时直接调用 `readPage`。
- 目标不明确时调用一次 `searchNotes`。
- 只读取回答所需的页面。
- 搜索无结果时如实说明。

## 限制

- 不搜索其他笔记本或目录。
- 不批量读取页面。
- 不为当前页润色任务搜索。
