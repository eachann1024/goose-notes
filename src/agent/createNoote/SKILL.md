# 新建笔记

## 适用

- 用户明确要求新建、生成或另存为一篇笔记。
- 结果应写入当前绑定笔记本的新页面。

## 不适用

- 用户要求修改当前页面。改用 `updateNote`。
- 用户只想讨论、总结或获取建议。改用 `chat`。

## 执行

- 信息不足时先询问必要信息。
- 确定标题和完整正文。
- 调用 `createPage` 一次。
- 正文首行不重复标题。
- 未经用户要求，不读取或改动现有页面。

## 完成

- 工具返回成功后，用一句话说明已创建。
- 工具失败时说明原因，不重复写入。
